spring:
    datasource:
        url: jdbc:postgresql://localhost:5433/pas
        username: emin
        password: 1111
        driver-class-name: org.postgresql.Driver
    jpa:
        generate-ddl: true
        hibernate:
            ddl-auto: create
    liquibase:
        change-log=classpath: /db/changelog/changelog-master.xml
    logging: level.liquibase = INFO